# Paste into one Kaggle cell after the regional branch offline setup.
# Reuses a tuned teacher cache; creates a NEW regional sketch target cache.
from datetime import datetime
REGIONAL_RUN = 'regional_av_sketch_2x2_s42_' + datetime.now().strftime('%Y%m%d_%H%M%S_%f')

%cd /kaggle/working/KD-SBIR-AVKD

!python -m src.train \
    --root /kaggle/input/datasets/b20dccn616nguynhutun/sketchy/Sketchy \
    --dataset sketchy_1 \
    --epochs 5 \
    --workers 8 \
    --batch_size 64 \
    --test_batch_size 1024 \
    --n_ctx_visual 3 \
    --prompt_depth 12 \
    --teacher_pretrain_epochs 1 \
    --teacher_cache_path /kaggle/working/teacher_cache/sketchy1_teacher_1ep.pt \
    --teacher_pretrain_batch_size 64 \
    --teacher_n_ctx_visual 10 \
    --teacher_prompt_depth 12 \
    --teacher_prompt_std 0.02 \
    --teacher_prompt_lr 3e-2 \
    --teacher_prompt_seed 42 \
    --teacher_prompt_gradient_checkpointing \
    --teacher_momentum 0.9 \
    --teacher_weight_decay 1e-3 \
    --lambda_teacher_retrieval 1.5 \
    --teacher_triplet_margin 0.2 \
    --lambda_domain 3.0 \
    --lambda_modality 1.0 \
    --photo_text_kd_temperature 0.15 \
    --sketch_text_kd_temperature 0.02 \
    --lambda_av 1.0 \
    --av_objective regional_cosine \
    --av_modality sketch_only \
    --av_region_grid 2 \
    --lambda_global_feature 0 \
    --av_cache_path /kaggle/working/teacher_cache/sketchy1_regional_av_sketch_2x2.pt \
    --av_teacher_batch_size 8 \
    --lr 1e-2 \
    --momentum 0.9 \
    --weight_decay 5e-4 \
    --seed 42 \
    --exp_name {REGIONAL_RUN} \
    --progress
